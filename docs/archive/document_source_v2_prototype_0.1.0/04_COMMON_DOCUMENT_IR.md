# 04 — Common Document IR

L'IR commune expose les unités utiles aux étapes aval : blocs et spans de texte,
tables, cellules, formules, images, annotations et unités protégées. Chaque unité
IR référence au moins une unité native source.
